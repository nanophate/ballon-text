%body.container
  %h1.header セリフ付き画像作成SNS
  %p.center 好みの画像を選択してセリフをつけよう！
  %div.docs-section
    %div.row
      - @images.each do |image|
        %div.four.columns
          = link_to image_tag(image[:url]), new_image_path(image_url: image[:url])
  %p.center 最近作られたセリフ付き画像
  %div.docs-section
    %div.row
      - if @caption.exists?
        - @caption.each do |c|
          %div.four.columns
            = link_to image_tag(c.image)
